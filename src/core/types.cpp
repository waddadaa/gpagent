#include "gpagent/core/types.hpp"

namespace gpagent::core {

// Most type implementations are in the header as inline functions
// This file is for any non-inline implementations if needed

}  // namespace gpagent::core
