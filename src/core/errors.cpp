#include "gpagent/core/errors.hpp"

namespace gpagent::core {

// Most error implementations are in the header as inline functions
// This file is for any non-inline implementations if needed

}  // namespace gpagent::core
