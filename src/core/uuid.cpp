#include "gpagent/core/uuid.hpp"

namespace gpagent::core {

// Most UUID implementations are in the header as inline functions
// This file is for any non-inline implementations if needed

}  // namespace gpagent::core
